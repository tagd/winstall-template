(x-mouseButtonControl
:config 
(config_file)
)

(rambox-pro
:config
(settings > (show in tray icon, startup:minimised)
)

(powertoys
:config
(setq run_as_admin t)
(setq fancyzones (
   use-non-primary-mouse-button
Move-newley created windows to last known zone))
)

(autohotkey(AHK)
:config
(AHK run on startup:
Win+R shell:startup  + RET
Add a shortcut to the script to this file)
)

(ms-teams
:config
(login)
settings>open in background)

(coldturkey
:config
(blocks_file))

(bitwarden
:config
unlock-w-pin t
unlock-w-windows-hello t
close-to-tray-icon t)
